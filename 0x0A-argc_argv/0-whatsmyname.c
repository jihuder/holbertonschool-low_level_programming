#include <stdio.h>

/**
 * main - function
 * @argc: matrix of pointers
 * @argv: saving the number of arguments
 * Return: 0
 */

int  main(int argc, char **argv)
{
	printf("%s\n", argv[0]);
	(void)argc;
	return (0);
}
